/*!
 * Bootstrap-select v1.7.0 (https://developer.snapappointments.com/bootstrap-select/)
 *
 * Copyright 2013-2021 Silvio Moreto and contributors
 * Licensed under the MIT license (https://github.com/snapappointments/bootstrap-select/blob/master/LICENSE)
 */

(function (factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD. Register as an anonymous module.
        define(['jquery'], factory);
    } else if (typeof module === 'object' && module.exports) {
        // CommonJS
        module.exports = factory(require('jquery'));
    } else {
        // Browser globals
        factory(jQuery);
    }
}(function ($) {

    var BootstrapSelect = function (element, options) {
        this.options = $.extend({}, $.fn.bootstrapSelect.defaults, options);
        this. $element = $(element);
        this.init();
    };

    BootstrapSelect.prototype = {
        init: function () {
            this.setData();
            this.buildList();
            this.bindEvents();
        },
        setData: function () {
            this.options.data = this.$element.data();
            this.options.maxOptions = this.options.maxOptions || this.$element.attr('data-max-options');
            this.options.selectedTextFormat = this.options.selectedTextFormat || this.$element.attr('data-selected-text-format');
            this.options.title = this.options.title || this.$element.attr('data-title');
            this.options.header = this.options.header || this.$element.attr('data-header');
            this.options.description = this.options.description || this.$element.attr('data-description');
            this.options.tickIcon = this.options.tickIcon || this.$element.attr('data-tick-icon');
            this.options.tickIconHeight = this.options.tickIconHeight || this.$element.attr('data-tick-icon-height');
            this
