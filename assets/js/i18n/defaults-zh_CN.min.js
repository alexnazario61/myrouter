/*!
 * Bootstrap-select v1.6.3 (http://silviomoreto.github.io/bootstrap-select/)
 *
 * Copyright 2013-2014 bootstrap-select
 * Licensed under MIT (https://github.com/silviomoreto/bootstrap-select/blob/master/LICENSE)
 */

// Improved default settings with i18n support
(function ($) {
    $.fn.selectpicker.defaults = {
        noneSelectedText: "No items selected", // Default English text
        noneResultsText: "No matches found", // Default English text
        countSelectedText: function (total, selected) {
            return selected + " of " + total + " selected"; // Custom format with total and selected count
        },
        maxOptionsText: [
            "Limit reached (max {n} items)", // Default English text for single select
            "Group limit reached (max {n} groups)" // Default English text for multiple select
        ],
        multipleSeparator: ", " // Default separator for multiple selected items
    };
}(jQuery));
