# 104 = Caixa Econômica

# 'codigo_cedente' field definition
codigo_cedente = {
  "pos": [18, 33],      # Position in the record (start and end)
  "picture": "X(16)",   # Data type: 16 alphanumeric characters
  "default": ""         # Default value if no data is provided
}

# 'brancos01' field definition
brancos01 = {
  # Complemento De Registro
  "pos": [34, 37],      # Position in the record (start and end)
  "picture": "X(04)",   # Data type: 4 alphanumeric characters
  "default": ""         # Default value if no data is provided
}

# 'nosso_numero' field definition
nosso_numero = {
  # Identificação Do Título No Banco
  "pos": [63, 73],      # Position in the record (start and end)
  "picture": "9(11)",   # Data type: 11 numeric characters
}

# ... Remaining field definitions follow the same pattern
